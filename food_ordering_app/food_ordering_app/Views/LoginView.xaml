<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:food_ordering_app.ViewModels"
             x:Class="food_ordering_app.Views.LoginView"
             Title="Đăng nhập"
             BackgroundColor="White"
             Visual='Material'>
    <ContentPage.BindingContext>
        <vm:LoginViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid Padding='20' VerticalOptions='Center'>
            <Grid.RowDefinitions>
                <RowDefinition Height='Auto' />
                <RowDefinition Height='*' />
            </Grid.RowDefinitions>

            <StackLayout Grid.Row='0' VerticalOptions='Center'>
                <Image Source='logo.png'
                       WidthRequest='100' HeightRequest='200'/>

                <Label Text='Đăp nhập' FontFamily='CabinSketch'
                       TextColor='Purple' FontSize='40' HorizontalOptions='Center' />
            </StackLayout>

            <StackLayout Grid.Row='1' Spacing='10'>
                <Entry Placeholder='Nhập tên đăng nhập' Text='{Binding Username}' />
                <Entry Placeholder='Nhập mật khẩu' Text='{Binding Password}' IsPassword='True' />

                <Button Text='Đăng nhập' HorizontalOptions='Center' BackgroundColor='Purple'
                        TextColor='White' WidthRequest='200' HeightRequest='60'
                        CornerRadius='20'
                        Command='{Binding LoginCommand}'/>

                <Label Text='Đăng kí mới :'/>

                <Button x:Name="btnRegister" Text='Đăng kí' HorizontalOptions='Center' BackgroundColor='DarkOrange'
                        TextColor='White' WidthRequest='200' HeightRequest='60'
                        CornerRadius='20'
                        Clicked="btnRegister_Clicked"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>